import { useParams } from "react-router-dom";


const EnrollClassDetails = () => {
    const {id} = useParams()
    // console.log(id);

    
    return (
        <div>
            EnrollClassDetails
        </div>
    );
};

export default EnrollClassDetails;